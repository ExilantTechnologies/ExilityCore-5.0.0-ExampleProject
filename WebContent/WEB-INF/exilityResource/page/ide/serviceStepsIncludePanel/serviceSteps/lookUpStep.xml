<displayPanel name="lookUpStep" description=""
	elementsPerRow="2"  hidden="true">
	<elements>
		<assistedInputField name="lookUpStepSource"
			description="This is must enter for numeric. But we wil not be able to use basedOnField because it is for two values. Hence we use a validation function"
			isRequired="false" dataElementName="name" label="Source:" />

		<textElement cssClassName="note" label="How severe is the error??" />

		<assistedInputField name="lookUpStepColumnNames"
			description="This is must enter for numeric. But we wil not be able to use basedOnField because it is for two values. Hence we use a validation function"
			isRequired="false" dataElementName="name" label="Column Names:" />

		<textElement cssClassName="note"
			label="Where should the control go? this will be another step in the service. The label name comes here" />


		<assistedInputField name="lookUpStepPrefix"
			description="This is must enter for numeric. But we wil not be able to use basedOnField because it is for two values. Hence we use a validation function"
			isRequired="false" dataElementName="subType" label="Prefix:" />

		<textElement cssClassName="note"
			label="Where should the control go? this will be another step in the service. The label name comes here" />

		<assistedInputField name="lookUpStepSelectionCriterion"
			description="This is must enter for numeric. But we wil not be able to use basedOnField because it is for two values. Hence we use a validation function"
			isRequired="false" dataElementName="subType" label="Selection Criteria:" />

		<textElement cssClassName="note"
			label="Where should the control go? this will be another step in the service. The label name comes here" />

	</elements>
</displayPanel>